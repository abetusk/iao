todo
---

* base templates
* ~symmetry weighting~
* ~animation randomness~
* ~color~
* ~make sure non trivial realization~
  - ~check for empty, redo~
  - ~reject with some prob if max level < threshold~


